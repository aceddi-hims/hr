service cloud.firestore {
  match /databases/{database}/documents {
    // Rules for all documents in the database.

    match /{document=**} {
      allow read, write: if request.auth != null; // Allow read/write if the user is authenticated
    }
  }
}